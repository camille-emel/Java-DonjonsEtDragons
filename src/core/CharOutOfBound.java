package core;

/**
 * The type Char out of bound.
 */
public class CharOutOfBound extends Exception {
    /**
     * Instantiates a new Char out of bound.
     *
     * @param message the message
     */
    public CharOutOfBound(String message) {
        super(message);
    }
}
